<div class="row text-center">
	<div class="mp_squares squares-gray">
		<div id="element1" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element1.target %></p>
			</div>
			<% if element_select && element1 %>
				<%= hidden_field_tag :element_id, element1.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
	<div class="mp_squares squares-gray">
		<div id="element2" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element2.target %></p>
			</div>
			<%if element_select && element2 %>
				<%= hidden_field_tag :element_id, element2.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
	<div class="mp_squares squares-gray">
		<div id="element3" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element3.target %></p>
			</div>
			<%if element_select && element3 %>
				<%= hidden_field_tag :element_id, element3.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
</div>
<div class="row text-center">
	<div class="mp_squares squares-gray">
		<div id="element4" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element4.target %></p>
			</div>
			<%if element_select && element4 %>
				<%= hidden_field_tag :element_id, element4.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
	<div class="mp_squares squares-white">
		<div id="element_center" class="box" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= center_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element5.target %></p>
			</div>
		</div>
	</div>
	<div class="mp_squares squares-gray">
		<div id="element5" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element6.target %></p>
			</div>
			<%if element_select && element6 %>
				<%= hidden_field_tag :element_id, element6.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
</div>
<div class="row text-center">
	<div class="mp_squares squares-gray">
		<div id="element6" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element7.target %></p>
			</div>
			<%if element_select && element7 %>
				<%= hidden_field_tag :element_id, element7.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
	<div class="mp_squares squares-gray">
		<div id="element7" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element8.target %></p>
			</div>
			<%if element_select && element8 %>
				<%= hidden_field_tag :element_id, element8.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
	<div class="mp_squares squares-gray">
		<div id="element8" class="box el_select" target="#input-modal">
			<div class="box-header">
				<p class="element_title"><%= around_text %></p>
			</div>
			<div class="box-body">
				<p class="text"><%= element9.target %></p>
			</div>
			<%if element_select && element9 %>
				<%= hidden_field_tag :element_id, element9.element_id %>
				<%= hidden_field_tag :user_id, current_user.id %>
			<% end %>
		</div>
	</div>
</div>
